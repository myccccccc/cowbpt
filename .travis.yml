language: cpp

sudo: required

compiler:
- gcc

env:
- PURPOSE=compile
- PURPOSE=unittest

before_install:
- sudo apt-get update -y
- sudo apt-get install -y libc6-dev

install:

before_script:

script:
- sh build_in_travis_ci.sh

after_failure: